services:
  app:
    image: ghcr.io/java-masterskaya/room-booking-system:${TAG}
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: always

networks:
  room-booking-network:
    external: true
